# Changelog

---

## v1.5 — قاعدة البيانات والإحصائيات

### ما الجديد

**قاعدة بيانات Supabase**
- تسجيل كل مستخدم عند أول تفاعل
- حفظ كل طلب مع النوع والنتيجة ووقت الاستجابة
- Rate limiting حقيقي يصمد عند إعادة تشغيل البوت

**أمر `/stats` للمطور**
- إجمالي المستخدمين والمستخدمين الجدد اليوم
- إجمالي الطلبات ونسبة النجاح
- متوسط وقت الاستجابة
- عدد طلبات التحليل مقابل Patch
- متاح فقط للمطور عبر `ADMIN_USER_ID`

**تحسينات أخرى**
- قياس وقت الاستجابة لكل طلب
- Bot يعمل بشكل كامل إذا كانت Supabase غير متصلة (fail open)

### الملفات الجديدة
| الملف | الوصف |
|-------|-------|
| `database.py` | Supabase client وكل عمليات قاعدة البيانات |
| `schema.sql` | SQL لإنشاء الجداول — يُشغَّل مرة واحدة في Supabase |

### الملفات المحدّثة
| الملف | التغييرات |
|-------|-----------|
| `config.py` | إضافة `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `ADMIN_USER_ID` |
| `bot.py` | تسجيل المستخدمين، تسجيل الطلبات، أمر `/stats` |
| `rate_limiter.py` | DB-backed مع in-memory fallback |
| `messages.py` | إضافة رسائل الإحصائيات |
| `requirements.txt` | إضافة `supabase>=2.0.0` |
| `.env.example` | إضافة متغيرات Supabase |

---

## v1.0 — الإصدار الأولي

- تحليل الصور بـ Claude AI وتوليد برومبت احترافي
- دعم 8 أنواع صور
- معالجة ذكية للوجوه (4 أوضاع)
- وضع Patch لإصلاح النتائج الفاشلة
- اشتراط الانضمام لقناة تيليجرام
- Rate limiting
- نشر على Railway
